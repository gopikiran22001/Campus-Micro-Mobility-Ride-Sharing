rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEBUG MODE - Allow all operations for testing
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
